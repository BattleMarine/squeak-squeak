# Unity 작업 지침

## 기본 원칙
- 답변과 작업 기록은 한국어로 간결하게 작성한다.
- 작업 전 README와 관련 문서를 읽고, 실제 파일·에디터 상태를 확인한다.
- 기존 구조와 규칙을 따르고 요청 범위에 필요한 변경만 수행한다.
- 사용자 변경사항을 보존한다. 이미 승인된 작업은 반복 확인 없이 진행한다.

## Unity Tool 사용
- 사용 가능한 Unity Tool/MCP를 탐색하고 설명·입력 스키마를 확인한다. 도구명이나 기능을 추측하지 않는다.
- 먼저 읽기 도구로 연결된 프로젝트 경로, 에디터 상태, 활성 씬을 확인한다. 다른 프로젝트에 작업하지 않는다.
- 패키지 설치만으로 연결됐다고 판단하지 않는다. 실제 조회 성공으로 연결을 검증한다.
- 씬·오브젝트·컴포넌트·프리팹·에셋 편집은 목적에 맞는 Unity Tool을 우선한다. C# 파일은 직접 편집할 수 있다.
- 도구가 없거나 연결되지 않으면 가능한 파일 작업을 진행하고, 남은 에디터 작업을 명시한다. 수행하지 않은 조작을 완료했다고 보고하지 않는다.
- 제공되지 않는 편집 기능은 지원되는 Editor API 실행 도구나 작은 Editor 스크립트로 보완한다. 임시 도구는 변경 범위를 제한한다.
- 패키지나 MCP를 요청 범위 밖에서 추가·교체하지 않는다. 기존 연결이 있으면 먼저 활용하거나 진단한다.

## 씬·오브젝트 편집 절차
1. **조회:** 대상 씬과 계층, 오브젝트 경로·ID, 컴포넌트, 현재 속성·참조를 확인한다. 이름만으로 대상을 결정하지 않는다.
2. **상태 확인:** Play Mode, 컴파일·임포트 진행, 저장되지 않은 변경을 확인한다. 씬 전환·재로드로 사용자 작업을 잃지 않도록 한다.
3. **범위 결정:** 씬 인스턴스, 프리팹 원본, 프리팹 Variant 중 수정 대상을 구분한다. 공유 에셋 변경의 영향 범위를 확인한다.
4. **편집:** 지원되는 도구로 생성·배치·부모 변경·컴포넌트 추가·속성 설정·참조 연결을 수행한다. Transform의 로컬/월드 좌표를 구분한다.
5. **재조회:** 반환 결과와 실제 상태를 확인한다. 리로드·재생성 후에는 ID를 다시 조회한다. 참조, 활성 상태, Missing Script도 점검한다.
6. **저장:** 의도한 씬·에셋만 저장한다. 관련 없는 프리팹 Override를 일괄 적용하거나 사용자 변경을 폐기하지 않는다.
7. **검증:** 변경 대상에 맞는 Console 확인과 Play Mode 동작 검증을 수행한다. 필요한 경우 저장 후에도 설정이 유지되는지 확인한다.

- 독립적인 조회는 묶고, 편집과 후속 확인은 순서대로 수행한다. 대규모 계층 전체보다 필요한 대상과 속성을 조회한다.
- 도구 오류는 원인과 현재 상태를 확인한 뒤 재시도한다. 동일한 실패 호출을 반복하지 않는다.
- 사용자 작업과 충돌하는 Play Mode 전환·씬 종료는 먼저 조율한다. Play Mode에서 조정한 영구 설정은 Edit Mode에 반영하고 저장한다.
- Editor 코드를 작성할 때 Undo, 직렬화, 프리팹 변경 기록과 저장 처리를 해당 Unity API에 맞게 적용한다.

## 코드·에셋 관리
- Unity 버전, 렌더 파이프라인, 입력 시스템, 패키지와 Assembly Definition을 확인하고 기존 구성에 맞춘다.
- 조정 가능한 값은 적절한 직렬화 필드로 노출하고 이름·단위·설명을 명확히 한다. Editor 전용 코드는 런타임 코드와 분리한다.
- 직렬화 필드·클래스 변경 시 저장된 값과 컴포넌트 참조의 호환성을 확인한다.
- 에셋 이동·이름 변경은 에디터 도구를 우선하며 `.meta`와 GUID를 보존한다.
- `.unity`, `.prefab`, `.asset`의 직접 텍스트 편집은 최소화한다. 불가피하면 직렬화 구조와 GUID/fileID를 확인하고 에디터에서 검증한다.
- `Library`, `Temp`, `Logs`, `obj` 등 생성물을 직접 수정하거나 버전 관리에 추가하지 않는다. 비밀정보와 개인 환경 설정도 제외한다.

## 검증·보고
- 코드 변경 후 Unity 컴파일·임포트가 끝난 다음 Console을 확인한다. 기존 오류와 새 오류를 구분하고 확인 전 로그를 지우지 않는다.
- 씬 작업은 컴포넌트 부착·Inspector 참조·이벤트 연결까지 확인한다. 코드 작성만으로 기능 완료를 판단하지 않는다.
- 변경 위험에 맞는 EditMode/PlayMode 테스트나 수동 재현을 수행한다. 빌드 관련 변경·마일스톤에서는 대상 플랫폼 빌드를 검증한다.
- 외부 C# 빌드 성공만으로 Unity 씬·실행 검증을 대체하지 않는다. 실행하지 못한 검증은 명시한다.
- 결과는 변경 내용, 검증 결과, 남은 작업으로 요약한다. 수동 확인이 필요하면 씬·대상·조작 순서를 안내한다.
- 실행 방법이나 구성·동작이 바뀌면 관련 문서를 갱신한다. 상세 계층과 개발 이력은 별도 문서에 두고 이 파일은 짧게 유지한다.

## Git·GitHub
- 작업 전 현재 브랜치와 `git status`를 확인한다. 원격 작업 전 대상 저장소·remote·기준 브랜치를 확인하고 이름을 추측하지 않는다.
- GitHub 연동 도구나 `gh`가 제공되면 지원 기능과 인증 상태를 확인해 사용한다. 저장소의 기여 지침·PR 템플릿·CI 설정을 따른다.
- 브랜치를 만들 때 기존 명명 규칙을 따르며, 별도 규칙이 없으면 `codex/<작업명>`을 사용한다.
- 커밋은 작업 관련 파일만 명시적으로 stage하고 diff를 확인한다. 사용자 변경을 섞지 않으며 에셋에 필요한 `.meta`도 함께 포함한다.
- 커밋 메시지 작성·검토 및 커밋 수행 시 `$korean-git-commit` 스킬을 사용한다. 위치는 `$CODEX_HOME/skills/korean-git-commit/SKILL.md`이며, 미설정 시 `~/.codex`를 기준으로 찾는다.
- 기본 형식은 `type: [영역] 변경 요약` + 빈 줄 + `- `로 시작하는 한국어 변경 요약 3~4개다. `docs`·`feat`·`fix` 등은 변경 목적에 맞추고 영역은 필요할 때만 붙인다. 스킬이 없으면 이 형식을 적용하고 미설치를 알린다.
- 커밋·push·PR 생성은 사용자가 요청하거나 승인한 범위에서 수행한다. 기존 승인은 유지하되 로컬 수정 요청만으로 원격 게시를 가정하지 않는다.
- PR은 문제와 변경 후 동작, 검증 결과, 남은 제한을 간결하게 설명한다. 실행하지 않은 테스트를 통과했다고 쓰지 않는다.
- push 후 CI가 작업 범위에 포함되면 결과를 확인하고 실패 원인을 조사한다. CI 통과와 Unity에서 직접 확인한 결과를 구분한다.
- 충돌은 양쪽 의도를 확인해 해결한다. 씬·프리팹 충돌은 한쪽으로 일괄 덮어쓰지 말고 참조와 에디터 상태를 검증한다.
- merge, force push, 원격 브랜치 삭제, 이력 재작성은 명시적 요청·승인 없이 수행하지 않는다. 실패한 검사를 우회하지 않는다.
